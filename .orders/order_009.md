今回の実験結果に基づき、以下の3点の修正を提案します。これらの修正はEx003に対して直接
反映するのではなく、Ex004として新規実装し、両者の結果を比較できるようにしてください。

1. M=5ワーカーの分散環境模擬とBatch Normalizationの挙動

論文の設定（M=5ワーカー）を再現するため、`data.py`または`train.py`のミニバッチ処理の中で、
1つのグローバルミニバッチ（サイズ128）を5つのサブバッチ（各25〜26程度）に分割し、5ワーカー
それぞれのローカルなBatch Normalizationの挙動を模擬してください（またはそれに相当する形で
正しく模擬してください）。各サブバッチの勾配を計算し、それらを平均（集約）してパラメータ更新に
用いることで、5ワーカーの分散環境におけるBatch Normalizationの統計量や勾配のノイズ・挙動を
正確に再現してください。

2. フル勾配計算時のBatch Normalization統計量の破損の修正

`compute_full_gradient_and_metrics`は現在`model.train()`を呼び出しており、フル勾配（スナップ
ショット勾配）を計算するためのデータセット全体の走査中に、Batch Normalizationの移動平均統計量
が動的に更新され続けてしまい、スナップショット評価としての数学的整合性が壊れています。これを
修正してください。具体的には、パラメータの勾配（`.grad`）の計算は有効にしたまま、Batch
Normalizationのrunning_mean/running_varが上書きされないようにしてください（例えば、パラメータ
勾配計算についてはtrain()相当のセマンティクスを保ちつつBatch Normalization層だけをeval()に
設定する、あるいは勾配計算前のno_gradの事前パスでBatch Normalizationの統計量を固定/修正する、
など）。

3. universal adversarial perturbation sigmaに対する勾配のスケールの修正

`MinMaxResNet18`において、共有パラメータsigma（形状(3, 32, 32)）に対する交差エントロピー損失の
勾配はバッチ内の全サンプルからbackpropagateされ合算されますが、正則化項
-lambda2/2*||sigma||^2の勾配はサンプル数に依存しないため、`reduction="mean"`を用いる誤差関数
とのスケールの不整合が生じています。`backward_minmax_objective`内で、sigmaの正則化項の勾配の
スケール・符号反転の扱いを見直し、変分不等式の定式化F_i(z)およびサンプル平均のスケールと整合する
ように修正してください。

修正後、全ての手法（SGD、SVRG、NFG SVRG、ASAI SVRG）および全てのSeed（5種類）で再実験を行い、
ログを保存してください。

ex004としてこれを実装してください。

---

## 解釈・保管メモ

ユーザーからチャットで指示された内容をそのまま上記に記録した．以下のように解釈して作業する．

- Ex003（`.orders/order_007.md`）は，NFG SVRG原論文が想定する「M=5ワーカーによる分散環境」を
  単一プロセスでのミニバッチ学習として簡略化していた．この簡略化が，Ex003・order_008（batch=1
  仮説）のいずれでも解消できなかった原論文との乖離（ASAI/NFG SVRGの発散）の一因である可能性を
  検証する．
- Ex003の実装（`programs/ex003_cifar10_resnet_minmax/`）はそのまま保持し，新規に
  `programs/ex004_cifar10_resnet_minmax/` を作成して3点の修正を実装する．ミニバッチサイズは
  128を維持する（ユーザーの明示的指示）．
- 修正1（M=5ワーカー模擬）：グローバルミニバッチ（サイズ128）を`M_WORKERS=5`個のサブバッチ
  （`torch.chunk`で分割，各サブバッチはサイズ約25〜26）に分割し，各サブバッチを独立に
  forward・backwardすることで，各ワーカーが自身のローカルミニバッチの統計量でBatch
  Normalizationを適用する挙動を模倣する．交差エントロピー損失の勾配は各サブバッチのサンプル数で
  重み付け平均し（グローバルバッチ全体の平均勾配と数学的に一致），正則化項の勾配はサブバッチ数に
  依らずグローバルミニバッチ全体に対して1回だけ加える（修正3の懸念に対応）．
  `backward_minmax_objective_distributed`関数として実装する．
- 修正2（フル勾配計算時のBatch Normalization統計量固定）：`compute_full_gradient_and_metrics`
  の`model.train()`を`model.eval()`に変更する．`.eval()`はautogradの勾配グラフ構築を妨げない
  ため，パラメータの`.grad`は通常通り計算されつつ，移動平均統計量の更新を防げる．
- 修正3（sigmaの正則化勾配のスケール）：修正1の設計（正則化項をグローバルミニバッチ全体に対して
  1回だけ加える）により構造的に解決される．データ項の勾配（サブバッチ重み付け平均）と正則化項の
  勾配（1回のみ）のスケールが一致する．
- 比較手法は原論文再現の主要4手法（SGD，SVRG，NFG，ASAI_SVRG）とし，Ex003と同じ5Seedで実行する．
- 実装完了後，`tests/`に新規テスト（サブバッチ集約が正しい平均勾配を再構成すること，フル勾配計算が
  Batch Normalization統計量を変更しないこと，正則化勾配がM_WORKERSに依らず1回分のスケールで
  加わること）を追加し，スモークテストの後に本実験（4手法 x 5Seed）を実行する．結果は
  `.reports/report_009.md`にまとめ，Ex003（`.reports/report_007.md`）との比較を含める．
